const title = "a-qq eshop";
const description = "A place to find something for yourself!";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    type: "website",
    title,
    description,
    siteName: title,
  },
};

export default nextSeoConfig;
